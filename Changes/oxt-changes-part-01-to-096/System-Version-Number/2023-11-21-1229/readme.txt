-- this adds a persistant global variable, which you can reference from either the messagebox (pictured) or you can run:-- put tSystemVersionNumber-- this will display the version number of the system reliably-- (testing a newly added patch system, a bit like github, so you can change just the lines of code you need)patch: home.livecodescript[Patch_Lin:] /opt/path-to-your-build/Toolset/[Patch_Win:] C:/path-to-your-build/Toolset/[Patch_Mac:] right-click the app > "show package contents" /Contents/Tools/Toolset/